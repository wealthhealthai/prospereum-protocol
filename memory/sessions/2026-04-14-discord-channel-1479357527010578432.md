## PHOENIX — discord-channel-1479357527010578432 — 2026-04-14

**Status:** active

---

## What Was Done

### StakingVault v3 — Shipped and Hardened

Started session recovering from a prior SIGTERM failure. The v3 StakingVault
(Synthetix-style passive accrual) had been written but the commit `a4e2b1f`
referenced in earlier reporting did not exist in git history.

Rebuilt and committed cleanly:

**`0859369`** — StakingVault v3 (Synthetix settlement model)
- `_settleFinishedEpochs(user)` called before every stake/unstake/claim
- `pendingRewards[user]` accumulates across all finalized epochs
- `claimAll()` and `claimStake(epochId)` pay from pendingRewards — no pre-claim required
- New staker cursor initializes to `currentEpochId()`, not epoch 0 (gas optimization)
- `MAX_SETTLE_EPOCHS = 52` — prevents unbounded settlement loop
- `checkpointUser(address)` retained for keeper compatibility (now a lazy-settle wrapper)
- 247/247 tests passing

**`601d0a0`** — Stranded pool fixes (ADJUDICATOR follow-up)
- Zero-staker epoch: `distributeStakerRewards()` now returns early without pulling
  tokens from RewardEngine if both `epochTotalPSREStaked` and `epochTotalLPStaked` are 0.
  Pool stays in caller — no PSRE permanently locked in StakingVault.
- `sweepUnclaimedPool(epochId, to)` (onlyOwner) added: governance can recover any
  sub-pool where `rewardPerToken == 0` (one pool type had no stakers that epoch).
- Updated `test_zeroStakers_poolNotPulled` to match new behavior.
- 247/247 tests passing

### ADJUDICATOR Review

An ADJUDICATOR sub-agent ran and reviewed `c538bc9` (old HEAD) rather than the new v3
code because `a4e2b1f` was not in git history. Key findings from its report:

- CRITICAL META: reviewed wrong commit (phantom hash). All "Hephaestus flags" were N/A.
- MAJOR: Stranded sub-pool rewards when one pool has no stakers → resolved in `601d0a0`.
- MINOR: `snapshotEpoch` in StakingVault has no epoch-end timestamp check (defense-in-depth).
  Currently mitigated by RewardEngine's own guard. Deferred — acceptable in trusted model.
- MINOR: Keeper dependency for passive staker contributions. Resolved by v3 settlement model.
- NITPICK: Contract version header still says "v2.0". Left as-is for now.
- VERDICT: CONDITIONAL PASS (stranded pool was the only required-before-mainnet item, now fixed).

### Complete BlockApex Response Message Drafted

Wrote a single, complete message covering all 22 original findings + 3 post-interim
self-identified vulnerabilities. Ready for Shu to send directly to BlockApex. Message includes:
- Each finding by number, severity, root cause summary, fix description, commit hash
- Post-interim Finding A: PSRE Mint-Cap DoS (`c538bc9`)
- Post-interim Finding B: ClaimStake Paradox / Yield Annihilation (`0859369`, `601d0a0`)
- Post-interim Finding C: Zero-Staker Epoch Pool Lock (`601d0a0`)
- Notes on partial findings (#4 fee tier / no TWAP, #17 EMA dispute position)
- Current HEAD `601d0a0`, 247/247 tests, Base Sepolia not redeployed yet

---

## Commit History (this session)

| Commit | Description |
|--------|-------------|
| `0859369` | StakingVault v3 — Synthetix settlement, passive staking works |
| `7c92921` | StakingVault v3 follow-up + xlsx files (ignore xlsx artifacts) |
| `601d0a0` | StakingVault stranded pool fixes — zero-staker no-pull + sweepUnclaimedPool |

HEAD: `601d0a0` | Tests: **247/247**

---

## Open Items / Blockers

### 🔴 Blocking Mainnet
1. **BlockApex final report** — expected any day. Message ready to send (Shu to send).
2. **Gnosis Safe creation** — Founder Safe + Treasury Safe. Still not created.
   Hard mainnet blocker. Shu + Jason need to do this at app.safe.global.

### 🟠 Decisions Needed
3. **Dev spec v3.3** — draft committed (`7bf9f68`), needs Shu + Jason sign-off before freeze.
4. **TWAP oracle for createVault** (#4 partial) — we mitigated with fee whitelist + floor;
   auditor may push back. Decision: accept current mitigation or add TWAP. Kin's position:
   current mitigation is sufficient and consistent with standard practice.
5. **EMA dispute (#17)** — two-pass EMA implemented. If auditors push back again, need
   Shu's decision on whether to accept their position or hold the line.

### 🟡 Ready to Execute on Signal
6. **Redeploy to Base Sepolia** — after final report clears. All 8 contracts need redeploy.
7. **Mainnet deploy script** — after Gnosis Safe addresses provided by Shu.
8. **Epoch 2 keeper** — automated April 18 20:00 UTC. No action needed.

---

## Needs Jason or Shu

- **Shu:** Send the drafted BlockApex response message (Kin posted it in Discord, ready to copy).
- **Shu + Jason:** Create Gnosis Safes at app.safe.global. Provide addresses to Kin.
- **Shu:** Approve dev spec v3.3 (`projects/prospereum/prospereum-dev-spec-v3.3.md`).
- **Jason:** Final approval on spec v3.3 once Shu signs off.
