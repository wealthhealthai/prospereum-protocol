# Memory — 2026-03-06

## Session Summary

First active session as Kin. Onboarding, setup, and knowledge base build.

---

## What Happened

### Onboarding
- Shiro introduced me to the #prospereum channel with full protocol context (3 messages: protocol overview, key numbers, contract list, hard rules)
- Jason tested awareness: confirmed I know who he is, confirmed I can see Shiro, confirmed web search capability
- Shiro verified my model config: `anthropic-sonnet46/claude-sonnet-4-6`, 1M context, 16K output tokens — correct

### Sub-agent Templates
- Jason asked Shiro to give me sub-agent templates
- Shiro wrote MACHINE, ADJUDICATOR, SCOUT, HEPHAESTUS, and Generic templates to my TOOLS.md (ARTIST excluded per Jason)
- Templates are now available for use immediately

### Knowledge Base Build (SCOUT)
- Jason requested deep Web3/tokenomics knowledge base
- Spawned SCOUT sub-agent 3 times — first two timed out at queue level (sessions had empty history, pre-execution timeout, likely load-related)
- Third spawn (3hr timeout) successfully did all research but hit 16K output token limit when trying to write the full document
- Root cause: `write` tool call with 8,000+ word content exceeds max output tokens — content parameter gets truncated mid-JSON
- **Fix:** Wrote the knowledge base directly in my main session using SCOUT's research
- **Result:** `knowledge/web3-knowledge-base.md` written (30KB, ~8,000 words)
- Updated `SOUL.md` to reference knowledge base as permanent expert context

### Dev Spec & Whitepaper Review
- Read full dev spec (v2.10, 11 pages) from workspace file
- Jason sent PDFs via Discord — read both in full using pypdf (no pdftotext installed)
- Whitepaper (v2.3, 9 pages) confirmed and fully intact
- Important: Whitepaper Section 6.2 shows **tier reward rates are 8% / 10% / 12%** (Bronze/Silver/Gold) — this reconciles with dev spec's `alphaBase × multiplier` formulation

### Pre-Build Clarification Questions
- Asked Jason 7 questions before starting any code
- 5 answered by documents on re-read
- **3 still open (awaiting Jason's reply tomorrow):**
  1. **Target chain** — which EVM chain? (assumption: Base or Arbitrum)
  2. **`buy()` router** — which DEX/router does PartnerVault call for USDC→PSRE swaps?
  3. **GitHub repo** — does one exist, or do I create fresh?

---

## Deployments

None. Pre-deployment phase. No contracts written yet.

---

## Decisions Made Today

None requiring decisions.md updates — all design decisions were pre-existing from dev spec.

**Pending decisions (unresolved, awaiting Jason):**
- Target chain
- PartnerVault buy() router / DEX
- GitHub repo location
- Upgradeability strategy (my default assumption: immutable for v1)

---

## What's Next

Once Jason answers the 3 open questions tomorrow:
1. Set up Hardhat project in `projects/prospereum/contracts/`
2. Write PSRE ERC-20 (simplest contract, good starting point)
3. Write PartnerNFT (EIP-5192 soulbound)
4. Write PartnerVaultFactory + PartnerVault
5. Write StakingVault
6. Write RewardEngine (most complex — save for last)
7. Full test suite covering all invariants from dev spec Section 11
8. ADJUDICATOR review before any testnet deploy

---

## Notes

- Sub-agent large file write issue: when content >~4,000 words, spawn sub-agent to do research only, then write files in main session where output isn't token-limited
- No git remote configured yet — will set up once GitHub repo is established
- `knowledge/web3-knowledge-base.md` is now permanent expert reference — read it when advising on protocol design, security, or tokenomics
