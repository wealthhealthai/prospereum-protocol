# Memory — 2026-05-08 (Friday)

## Session Summary

Week-in-review catch-up. Epoch 1 finalized clean. Nadir PSRE-native observations fixed. setSplit not yet executed (harmless for Epoch 1, still needed before Epoch 2). PHOENIX at 03:46 AM PDT.

---

## Week of May 4–8

### Epoch 1 — Finalized ✅ (May 6 03:52 UTC)
On-chain: `lastFinalizedEpoch = 1`, `firstEpochFinalized = true`, `T = 0`
- 0 PSRE minted — no active partners, no stakers
- Fix Post-C handled zero-staker case correctly: P_stakers = 0, no emission consumed
- Epoch 2 now running — closes **May 13 03:52 UTC**

### setSplit — NOT Executed
- On-chain: `psreSplit = 0.5e18`, `lpSplit = 0.5e18` (still 50/50 default)
- Shu never co-signed Founder Safe nonce 2
- **Harmless for Epoch 1:** T=0, zero partners, zero stakers → zero emission regardless of split
- **Still needed before Epoch 2 closes (May 13)** if any PSRE stakers appear

### Nadir PSRE-Native Observations Fixed (`0aba2e9`, May 4)
Nadir reviewed the PSRE-native refactor and flagged 6 items — all addressed:
- Obs #1: `setPsreMin()` missing event → `PsreMinUpdated` added
- Obs #2: migration calling pattern → documented in `executeSetFactory()` natspec
- Obs #3: `setFactory()` when paused → `executeSetFactory()` requires `whenPaused`
- Obs #4: orphaned EMA/sumR after migration → `clearVaultScores(address[])` added
- Obs #5: `setFactory()` timelock → full `schedule/cancel/execute` pattern with 7-day UPGRADE_TIMELOCK
- Obs #6: `isContract()` check → `require(_factory.code.length > 0)` in `scheduleSetFactory()`
- **250/250 tests passing**

---

## Protocol State (as of May 8)

| Item | Status |
|---|---|
| Epoch 1 | ✅ Finalized — 0 PSRE minted |
| Epoch 2 | Running — closes May 13 03:52 UTC |
| T (total emitted) | 0 |
| Partners | 0 registered |
| setSplit(1e18, 0) | ❌ Shu co-sign still pending — harmless so far, needed before May 13 |
| PSRE-native refactor | ✅ Shipped (`f4d4cc6`) + Nadir observations fixed (`0aba2e9`) |
| Tests | ✅ 250/250 |
| Genesis LP pool | ❌ Not yet created |
| Sablier vesting | ❌ Not yet set up |

---

## Open Items

### 🔴 Before May 13 (Epoch 2 close)
1. **Shu: co-sign setSplit** — Founder Safe nonce 2. Harmless this week, critical if stakers appear.

### 🟠 Shu (at his pace)
2. Genesis LP pool ($40K, Treasury Safe → Uniswap v3)
3. Unicrypt LP lock (24 months)
4. Sablier vesting stream (4.2M PSRE from Founder Safe)

### 🟠 Jason
5. Nadir closing message — send final commit hash (can be done anytime)
6. BlockApex badge on Prospereum website

### 🟡 Kin (waiting on resources)
7. Basescan contract verification (needs mainnet Basescan API key)
8. Mainnet upgrade to PSRE-native (once Safe batch timing confirmed)

### 🟡 Midas
9. Blocked by Olympus endpoints — Zeus Phase 2 dependency

---

## EOD Update (PHOENIX 01:49 AM May 9)

### Nadir Delta Cleared ✅ (`54d0e67`, `7a938c4`)
- All 6 PSRE-native observations cleared by Nadir
- Audit findings CSV updated with PSRE-delta #1-#6 (all cleared)
- MEMORY.md updated with audit status + open items
- Decision logged: PSRE-native refactor fully audited and cleared

### Factory Upgrade Staged (`be00860`)
3-step mainnet upgrade plan ready:
- `script/DeployFactoryUpgrade.s.sol` — deploy new RE impl + new Factory
- `audit/upgrade-step1-scheduleUpgrade.json` — Day 0: schedule RE upgrade (7-day timelock)
- `audit/upgrade-step2-executeUpgrade-scheduleSetFactory.json` — Day 7: execute RE + schedule factory swap
- `audit/upgrade-step3-executeSetFactory-wire.json` — Day 14: execute factory swap (pause → swap → unpause)

**Total time: 14 days. Can start any time. Zero partners = no migration pain.**

### Keeper Check
Epoch 2 closes May 13 03:52 UTC (~91 hours from PHOENIX)
